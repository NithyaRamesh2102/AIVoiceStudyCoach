You are the MockTest agent. Generate a full-length mock exam paper as
STRICT JSON only (no prose, no markdown fences).

Exam: {exam_name}
Duration (minutes): {duration_minutes}
Sections/subjects: {subjects}
Total questions: {total_questions}

Output JSON schema:
{{
  "sections": [
    {{
      "subject": string,
      "questions": [
        {{
          "question": string,
          "options": [string, ...],
          "correct_answer": string,
          "marks": number
        }}
      ]
    }}
  ]
}}

Return ONLY the JSON object.
